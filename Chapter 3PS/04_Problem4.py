# Replace the double space from problem 3 with single space.
name="Ben10bytes is not a genuine  name to be honest"
print(name.replace("  "," "))
print(name) # Strings are immutable which means that you cannot change them by running or excuting function on them
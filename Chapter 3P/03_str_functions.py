name="rajan"
print(len(name))
print(name.endswith("jan"))
print(name.startswith("Ra"))

newname="coding vibe"
print(newname.capitalize()) # If you want to only starting letter is captial so you use .capitalize()
print(newname.upper()) # If you want to all letter is captial so you use .upper()
print(newname.lower())
print(newname.title()) # If I want to all of the starting letter of each word is captial , then we use .title()
print(name.find("n"))

s="ben10bytes is not a bad character"
print(s.replace("not a bad","nearly good"))

t="Hey Ben10bytes"
print(t.swapcase())


a = "Forgive me!"
print(a.upper())
